#!/usr/bin/env bash

FOLDER="/home/bascht/Videos/Screenrecordings/$(date +'%Y-%m')"
mkdir -p "${FOLDER}"


NAME="${FOLDER}/$(date +%Y-%m-%d-%H%M%S)"
if [[ -n "${*}" ]]; then
	NAME="${NAME}-${*}";
fi;

wf-recorder -g "$(slurp-window)" -f "${NAME}.mp4"
echo -e "\n\n${NAME}.webm"
